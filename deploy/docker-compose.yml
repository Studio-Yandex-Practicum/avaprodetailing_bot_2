volumes:
  pg_data:

services:
  bot:
    build: ..
    restart: always
    command: "python main.py"
    env_file: ../.env

  db:
    image: postgres:13
    env_file: ../.env
    volumes:
      - pg_data:/var/lib/postgresql/data
